{% extends 'base.html.twig' %}

{% block title %}- Personnage{% endblock %}

{% block body %}

   <h1>Bienvenue {{capsuler.name}} </h1>

   <img src="{{ portraits.px256x256 }}"> 

   {# {{ dump(capsuler) }} #}

   {# {% for question in capsuler.questions %}

      {{ dump(question.title) }}

   {% endfor %} #}


   {# Objet de type Capsuler #}
   {# {{ dump(app.session.get('capsuler')) }} #}
   {# Objet de type EveOnlineResourceOwner fourni par le provider et l'authentification OAuth #}
   {# {{ dump(app.session.get('user'))}} #}

{% endblock %}
