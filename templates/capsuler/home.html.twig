{% extends 'base.html.twig' %}

{% block title %}- Personnage{% endblock %}

{% block body %}

   <h1>Bienvenue {{capsuler.name}} </h1>

   <img src="{{ portraits.px256x256 }}"> 
   {{ dump(capsuler) }}
   {{ dump(app.session) }}
   {{ dump(app.session.get('capsuler').questions) }}
   {{ dump(app.session.get('capsuler').questions) }}

{% endblock %}
